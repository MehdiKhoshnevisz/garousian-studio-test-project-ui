import styled from "styled-components";

const SectionWrapperStyled = styled.div({
  display: "flex",
  paddingTop: "2em",
  paddingBottom: "2em",
  alignItems: "center",
  justifyContent: "center",
});

export const SectionWrapper = SectionWrapperStyled;
